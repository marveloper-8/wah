import { createActionType } from "../../helpers/reduxHelpers";

export const ORGS = createActionType("ORGS");
export const WIDGETS = createActionType("WIDGETS");
export const HOOKSLOG = createActionType("HOOKSLOG");
export const HOOKS_PAGE = createActionType("HOOKS_PAGE");
